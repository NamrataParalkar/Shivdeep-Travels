"use client";
import AdminComplaintsPage from "../complaints/page";

export default function AdminFeedbackWrapper() {
  // The Admin Dashboard links to `/admin/feedback` — render the updated admin complaints/feedback UI here so the dashboard shows the new interface.
  return <AdminComplaintsPage />;
}
